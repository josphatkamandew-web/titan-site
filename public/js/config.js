// This file is regenerated at build time by build-config.js from the
// TITAN_API_BASE Netlify environment variable. This checked-in version
// is just a safe local-dev default so opening the site without running
// the build doesn't error out outright.
window.TITAN_API_BASE = "http://localhost:8000";
